TheTok App Store finalization trigger v1

Purpose: finalize iOS 1.0 metadata, attach build 2, provision an isolated App Review account, and submit to App Review after CI validation.

This file is intentionally one-shot. The production workflow accepts it only when it is first added to main.
