TheTok App Store finalization trigger v2

Purpose: retry iOS 1.0 finalization after correcting GitHub Actions runner-context scope.

This file is intentionally one-shot. The production workflow accepts it only when it is first added to main.
