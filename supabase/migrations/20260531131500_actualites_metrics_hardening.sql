-- Superseded migration marker.
--
-- This version may already exist in the remote Supabase migration history.
-- Keep this file in the repository so `supabase db push` can reconcile the
-- remote migration table with the local migration directory.
--
-- The actual corrected implementation lives in:
-- 20260531150500_actualites_weighted_rotation_and_conversion_attribution.sql

SELECT 1;
